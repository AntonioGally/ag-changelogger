name: "Change logger"
description: "Update the changelog by your PR informations"
inputs:
  changelogPath: # id of input
    description: "Path to your changelog file"
    required: true
    default: "./CHANGELOG.md"
  commitEmail:
    description: "Email that will be used to commit changes"
    required: true
  commitUserName:
    description: "UserName that will be used to commit changes"
    required: true

runs:
  using: "node16"
  main: "./dist/index.js"
